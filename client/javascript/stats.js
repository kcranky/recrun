import './../templates/stats.html';

import { Accounts } from 'meteor/accounts-base';

/*
AccountsTemplates.configure({
    texts: {
        title: {
            signIn: "Sign in to fetch your stats",
            signUp: "Sign up to store your stats"
        }
    }
});
*/